#include "Shader.h"

#include <ft2build.h>
#include ""

class SudokuGame
{
public:
	unsigned int lives = 3;
	unsigned int matrix[9][9] = { 0 };
	GLuint VAO, VBO;
	Shader* txtShader;
	Shader* gridShader;

	SudokuGame(unsigned int width, unsigned int height, GLuint vao, GLuint vbo, )
};
int main()
{
	Shader textShader
	GLuint VAO, VBO;
	
	return 0;
}